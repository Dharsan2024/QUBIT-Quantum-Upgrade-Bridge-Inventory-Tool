[project]
name = "qubit-risk"
version = "0.1.0"
description = "QUBIT risk (placeholder — see docs/design)"
requires-python = ">=3.12,<3.14"
license = "MIT"
dependencies = ["qubit-core"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/qubit_risk"]
