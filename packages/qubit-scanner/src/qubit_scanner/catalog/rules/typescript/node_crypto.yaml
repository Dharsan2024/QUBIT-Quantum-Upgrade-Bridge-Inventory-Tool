schema: qubit-rule/v1
language: typescript
# Same queries also compiled against the tsx grammar:
# tree-sitter treats TSX as its own language, and React components are
  # where browser-side crypto usually lives.
additional_languages: [tsx]
library:
  name: node:crypto
  # No import gate: resolve.py has no JS import-extraction query yet (see javascript/jwt.yaml), and
  # `crypto` is reachable as a global in modern Node without any import at all.
  detect_imports: []
rules:
  # TypeScript parity for the javascript/node_crypto.yaml pack. tree-sitter-typescript is a
  # separate grammar with its own rule set, so a .ts file matched NONE of the JS node-crypto
  # rules — a TypeScript backend using node:crypto scanned completely clean. The queries are
  # identical because both grammars share these call/member/object node shapes (verified).
  #
  # The algorithm name is read from the string argument rather than hardcoded per-rule, so one rule
  # covers every digest/cipher Node accepts and the canonical registry decides the verdict (doc 01
  # §4.2: rules only NAME algorithms, they never judge them). Unrecognised names surface as
  # UNKNOWN(...) rather than being dropped.
  - id: TS-NODE-CREATEHASH
    title: crypto.createHash(<digest>)
    match:
      query: |
        (call_expression
          function: (member_expression
            object: (identifier) @obj
            property: (property_identifier) @meth)
          arguments: (arguments (string (string_fragment) @alg))) @call
      where:
        - { capture: obj, equals: crypto }
        - { capture: meth, equals: createHash }
    extract:
      algorithm: { from: alg, resolve: capture-text }
    asset: { asset_type: algorithm-use, usage_context: hash }
    confidence: high
    examples:
      positive:
        - |
          import * as crypto from 'crypto';
          const h = crypto.createHash('md5').update(data).digest('hex');
        - |
          import * as crypto from 'crypto';
          const h = crypto.createHash('sha256').update(data).digest('hex');
      negative:
        - |
          import * as crypto from 'crypto';
          const h = crypto.randomBytes(32);

  - id: TS-NODE-CREATECIPHERIV
    title: crypto.createCipheriv / createDecipheriv(<cipher>, key, iv)
    match:
      query: |
        (call_expression
          function: (member_expression
            object: (identifier) @obj
            property: (property_identifier) @meth)
          arguments: (arguments (string (string_fragment) @alg) (_) (_))) @call
      where:
        - { capture: obj, equals: crypto }
        # createDecipheriv too: decryption is as much a crypto usage as encryption, and a
        # service that only ever DECRYPTS — an ingest or consumer tier, a very common
        # architecture — otherwise reported no cipher usage at all. Found by scanning a
        # telemetry ingest target that decrypts RC4 payloads and got zero cipher findings.
        - { capture: meth, in: [createCipheriv, createDecipheriv] }
    extract:
      algorithm: { from: alg, resolve: capture-text }
    asset: { asset_type: algorithm-use, usage_context: encryption-at-rest }
    confidence: high
    examples:
      positive:
        - |
          import * as crypto from 'crypto';
          const c = crypto.createCipheriv('aes-128-cbc', key, iv);
        - |
          import * as crypto from 'crypto';
          const c = crypto.createCipheriv('des-ede3-cbc', key, iv);
        - |
          import * as crypto from 'crypto';
          const d = crypto.createDecipheriv('rc4', key, iv);
      negative:
        - |
          import * as crypto from 'crypto';
          const h = crypto.createHash('sha256');

  - id: TS-NODE-CREATEHMAC
    title: crypto.createHmac(<digest>, key)
    match:
      query: |
        (call_expression
          function: (member_expression
            object: (identifier) @obj
            property: (property_identifier) @meth)
          arguments: (arguments (string (string_fragment) @alg) (_))) @call
      where:
        - { capture: obj, equals: crypto }
        - { capture: meth, equals: createHmac }
    extract:
      algorithm: { literal: HMAC }
    asset: { asset_type: algorithm-use, usage_context: token }
    confidence: high
    examples:
      positive:
        - |
          import * as crypto from 'crypto';
          const mac = crypto.createHmac('sha256', secret).update(msg).digest();
      negative:
        - |
          import * as crypto from 'crypto';
          const h = crypto.createHash('sha256').update(msg).digest();

  - id: TS-NODE-GENERATEKEYPAIR-RSA
    title: crypto.generateKeyPairSync('rsa', ...)
    match:
      query: |
        (call_expression
          function: (member_expression
            object: (identifier) @obj
            property: (property_identifier) @meth)
          arguments: (arguments (string (string_fragment) @alg) (_))) @call
      where:
        - { capture: obj, equals: crypto }
        - { capture: meth, in: [generateKeyPairSync, generateKeyPair] }
    extract:
      algorithm: { from: alg, resolve: capture-text }
    asset: { asset_type: algorithm-use, usage_context: kex }
    confidence: high
    examples:
      positive:
        - |
          import * as crypto from 'crypto';
          const { publicKey } = crypto.generateKeyPairSync('rsa', { modulusLength: 2048 });
        - |
          import * as crypto from 'crypto';
          const { publicKey } = crypto.generateKeyPairSync('ed25519', {});
      negative:
        - |
          import * as crypto from 'crypto';
          const mac = crypto.createHmac('sha256', secret);
